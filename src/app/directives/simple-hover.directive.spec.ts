import { SimpleHoverDirective } from './simple-hover.directive';

describe('SimpleHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleHoverDirective();
    expect(directive).toBeTruthy();
  });
});
